//import com.tasks.*

class BootStrap {

    def init = { servletContext ->
/*    	def event1 = new Categoria(categoria:'Pessoal')
    	if(!event1.save()){
			event1.errors.allErrors.each{error ->
			println "An error occured with event1: ${error}"
			}
		}
		def event2 = new Categoria(categoria:'Profissional')
    	if(!event2.save()){
			event2.errors.allErrors.each{error ->
			println "An error occured with event1: ${error}"
			}
		}*/
    }
    def destroy = {
    }
}
